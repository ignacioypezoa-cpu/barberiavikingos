import{NextResponse}from"next/server";import{prisma}from"@/lib/prisma";import{requireAdmin}from"@/lib/admin";export async function GET(){const a=await requireAdmin(true);if(a.error)return a.error;const rows=await prisma.customer.findMany({include:{appointments:{where:{status:"COMPLETED"},include:{service:true,barber:true},orderBy:{startAt:"desc"},take:1},_count:{select:{appointments:true,orders:true}}},orderBy:{updatedAt:"desc"}});return NextResponse.json(rows)}
