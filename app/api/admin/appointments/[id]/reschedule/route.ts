import{NextRequest,NextResponse}from"next/server";import{z}from"zod";import{prisma}from"@/lib/prisma";import{requireAdmin}from"@/lib/admin";import{isSlotAvailable}from"@/lib/appointments";const schema=z.object({startAt:z.string()});export async function PATCH(r:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireAdmin(true);if(a.error)return a.error;const p=schema.safeParse(await r.json());if(!p.success)return NextResponse.json({error:"Fecha inválida."},{status:400});const appointment=await prisma.appointment.findUnique({where:{id:(await params).id},include:{service:true}});if(!appointment)return NextResponse.json({error:"Reserva no encontrada."},{status:404});const startAt=new Date(p.data.startAt);const endAt=new Date(startAt.getTime()+appointment.service.duration*60000);if(!await isSlotAvailable(appointment.barberId,appointment.branchId,startAt,endAt,appointment.id))return NextResponse.json({error:"Horario no disponible."},{status:409});return NextResponse.json(await prisma.appointment.update({where:{id:appointment.id},data:{startAt,endAt,reminder24hSentAt:null,reminder2hSentAt:null,reminder30mSentAt:null}}))}
